
# 2026-09-21 Monday
## 今日任务清单
- [ ] 给OB引入混合检索和RRF

## 过程记录
### P001-
- **关联任务**：
- **触发场景**：
- **原始问题**：
- **解决方式**：
- **我的理解**：
- **拓展知识**：

1. 调研OB是否支持混合检索，是否支持RRF
2. 专有文档问答agent，是单agent还是多agent方案
3. 专有文档问答agent是昨天讨论的单文件文档问答方案吗，如果是，可以把rag_search和deep_reading工具合并到一个类里面，每次使用时初始化文件的file_id
4. 每份专有文档知识库以何种形式存在，通过向量数据里面的一个字段过滤还是分不同的partition还是何种方案。
5. 

在`DocumentChatToolkit`中：
1.rag_search工具的组成部分：
1️⃣：`self._build_search_params`,做两件事情，定制requests（request内部参数可以来自`DocumentChatToolkit`类的初始化），`self._build_requests`和拼接其他参数（同理也可以来自`DocumentChatToolkit`类的初始化），组成`self._retriever.search`的入参
2️⃣：`self._retriever.search`,使用`self._build_search_params`传入的参数在函数内部做hybird_search，reranker，top_k,threshold等，我们不关注细节，我们只管调用
3️⃣：做后处理，分别给模型和前端看。

2.deep_reading工具的组成部分：
1️⃣：`self._build_query_params`,使用来自`DocumentChatToolkit`类的初始化好的参数构造`self._retriever.query`的入参
2️⃣：`self._retriever.query`,使用`self._build_query_params`传入的参数在函数内部做查询，返回相关的数据
3️⃣：后处理

其中`self._retriever.search`和`self._retriever.query`一定会透传来自`steins-algorithm/third_party/steins-ai/steins_ai/storages/vectordb_storages/async_oceanbase.py`路径里面hybrid_search和query两个方法的所有入参，如果还有其他显示声明的入参，那么一定是在`self._retriever.search`和`self._retriever.query`内部被使用，而不是透传给上游。

理解我以上的需求以后，和我确认方案，然后重构现在的代码



## 每日复盘


```
[
	{
		"kb_id":"xxx"
	},
	{
		"fild_ids" ["x","x", "x"]
	}
]
```
filter=kb_id=1 or file_ids in [x,x,x] or kb_id=2
filter=kb_id in [1, 2] or file_ids in [x,x ,x ]